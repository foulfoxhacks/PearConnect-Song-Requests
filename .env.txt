# Copy to .env or run npm run setup (creates a private secret; never overwrites).
# Never commit your completed .env or include it in support screenshots.
YTMD_HOST=http://127.0.0.1:26538
YTMD_CLIENT_ID=ytmd-stream-bot
# Run npm run auth and approve the request in Pear Desktop.
YTMD_TOKEN=
# Timeout per player API call. No automatic retries for writes.
YTMD_TIMEOUT_MS=10000

# Request rules: 0 disables the corresponding limit.
COOLDOWN_SECONDS=60
MAX_SONG_SECONDS=420
# Best-effort duration-based request window, NOT exact playback queue ownership.
MAX_PER_USER=2
BLOCKLIST=

# Leave channel blank to disable. All three fields required when enabled.
TWITCH_CHANNEL=
TWITCH_USERNAME=
TWITCH_OAUTH=
# YouTube chat input is unofficial/read-only; replies go to the terminal.
YOUTUBE_CHANNEL_ID=

# TikTok -> TikFinity -> Streamer.bot -> local HTTP POST API.
# New installations use Simple; existing files without CONNECTION_MODE stay Advanced.
CONNECTION_MODE=simple
TIKFINITY_WS_URL=ws://127.0.0.1:21213/
# Deliberate enable: CLI --accept-requests or set true after testing.
REQUESTS_ENABLED=false
# This is NOT Pear Desktop's API port. 0 disables the HTTP bridge.
TIKFINITY_PORT=7280
# Match Streamer.bot's persisted global PearConnect.Secret. Setup generates this.
TIKFINITY_SECRET=
# True disables ALL player calls and Twitch/YouTube connections.
DRY_RUN=false

# No leading !. Configure matching TikFinity events separately.
CMD_REQUEST=sr
CMD_NOWPLAYING=np
CMD_QUEUE=queue
CMD_SKIP=skip
# Prefer tiktok:handle,twitch:login,youtube:UC_EXACT_CHANNEL_ID.
# Bare TikTok/Twitch usernames retained for compatibility; empty denies everyone.
SKIP_ALLOWLIST=
# Empty allows everyone; nonempty uses the same scoped identities as skip permissions.
REQUEST_ALLOWLIST=
