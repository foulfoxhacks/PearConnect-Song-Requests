{
  "name": "pearconnect-session-relay",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --ip 127.0.0.1 --port 8790",
    "types": "wrangler types",
    "check": "wrangler types && tsc --noEmit && wrangler deploy --dry-run --outdir dist",
    "test": "npm run check && node --test test/relay.test.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260905.1",
    "miniflare": "^5.20260903.0-alpha",
    "typescript": "^7.0.2",
    "wrangler": "4.129.0"
  }
}
